# Runtime dependencies (Python 3.13+)
aiogram>=3.15,<4
SQLAlchemy[asyncio]>=2.0.36,<3
asyncpg>=0.30.0
alembic>=1.14.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.1
# IANA time zone database: zoneinfo has no system tzdata on Windows
# or on slim images, and APP_TIMEZONE month boundaries depend on it.
tzdata>=2024.1
